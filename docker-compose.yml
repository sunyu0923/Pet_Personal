version: '3.9'
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: peti
      POSTGRES_PASSWORD: peti_dev
      POSTGRES_DB: peti
    ports:
      - "5432:5432"
    volumes:
      - peti_pgdata:/var/lib/postgresql/data

volumes:
  peti_pgdata:
